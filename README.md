# Psilon

Public content for the Psilon project is being prepared. The website, project
documentation, and downloadable releases will appear here shortly.

---

*Maintainer note (visible publicly, replaced in Phase 3): this repository's
content is generated from the private development repo's `public/` publishing
valve. The valve preserves relative paths — `public/X` in the private repo
becomes `/X` here. Only content deliberately placed in the valve is ever
published; sync rules live in the private repo's `docs/RELEASE_CONTRACT.md`.*
